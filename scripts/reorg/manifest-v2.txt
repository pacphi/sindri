# manifest-v2.txt — paths kept on `v2` after cutover.
# v2 stack: Bash + Docker. Maintenance lane.

LICENSE
README.md                  # = current v2/README.md, lightly edited
CHANGELOG.md               # = v2/CHANGELOG.md
RELEASE_NOTES.md           # = current RELEASE_NOTES.v2.md (renamed)

# All v2 source
v2/

# v2 examples (relocated from root examples/v2/ during cutover)
# build-branches.sh moves examples/v2/** → v2/examples/** before isolating
# (so post-cutover, examples live under v2/examples and the manifest matches v2/)

# Hygiene configs (per-branch copies)
.dockerignore
.gitignore
.markdownlint.json
.markdownlintignore
.prettierignore
.prettierrc
.shellcheckrc
.yamllint.yml
.husky/

# Shared dev tooling (Claude agents + extension-guide skills used on this branch)
.claude/agents/
.claude/skills/extension-guide/
.claude/skills/extension-guide-v2/
.claude/skills/sindri-extension-guide/

# AI tooling (authored fresh per branch during cutover)
# CLAUDE.md
# AGENTS.md
