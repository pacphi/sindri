# Sindri Console API — local development (pnpm dev)
# Copy to .env and fill in values before running locally.
#
#   cp .env.example .env
#
# For Docker Compose, use the root .env instead (see .env.example at v3/console/).

# ── Database (localhost — pnpm dev) ──────────────────────────────────────────
DATABASE_URL="postgresql://sindri:sindri@localhost:5432/sindri_console?schema=public"

# ── Redis (localhost — pnpm dev) ─────────────────────────────────────────────
REDIS_URL="redis://localhost:6379"

# ── Server ────────────────────────────────────────────────────────────────────
PORT=3001
NODE_ENV=development
LOG_LEVEL=debug

# ── CORS ─────────────────────────────────────────────────────────────────────
# Comma-separated list of allowed origins for browser clients.
CORS_ORIGIN="http://localhost:5173,http://localhost:3000"

# ── Security (generate with: openssl rand -hex 32) ───────────────────────────
JWT_SECRET="change-me"
SESSION_SECRET="change-me"

# Bootstrap API key — agents must present this key to register with the Console.
SINDRI_CONSOLE_API_KEY="dev-api-key-change-me"

# ── Retention (optional, defaults shown) ─────────────────────────────────────
# METRICS_RETENTION_DAYS=30
# LOGS_RETENTION_DAYS=14
# EVENTS_RETENTION_DAYS=90
# AUDIT_RETENTION_DAYS=365
