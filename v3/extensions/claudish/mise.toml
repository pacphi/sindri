# claudish.toml - OpenRouter model proxy for Claude Code
# Requires nodejs extension to be installed first
#
# IMPORTANT: Use pinned versions instead of "latest" for reliable installations
# query timeouts that can poison subsequent mise operations.

[tools]
# Claudish CLI - run Claude Code with OpenRouter models
"npm:claudish" = "4.6.6"
