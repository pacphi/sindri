metadata:
  name: ansible
  version: "13.5.0"
  description: "Ansible configuration management"
  license: GPL-3.0
  homepage: "https://ansible.com"
  tags:
    - cfg-mgmt

platforms:
  - os: linux
    arch: x86_64
  - os: linux
    arch: aarch64

install:
  apt:
    packages:
      - "ansible"
      - "ansible-lint"
    update_first: true

depends_on: []
