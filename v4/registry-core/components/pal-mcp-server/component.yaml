metadata:
  name: pal-mcp-server
  version: "1.0.0"
  description: "PAL MCP server"
  license: MIT
  homepage: "https://github.com/BeehiveInnovations/pal-mcp-server"
  tags:
    - mcp
    - pal

platforms:
  - os: linux
    arch: x86_64
  - os: linux
    arch: aarch64
  - os: macos
    arch: x86_64
  - os: macos
    arch: aarch64

install:
  script:
    install: "install.sh"
    timeout: "600"

depends_on:
  - "mise:python"
