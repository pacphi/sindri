metadata:
  name: xfce-ubuntu
  version: "1.0.0"
  description: "XFCE desktop for Ubuntu containers"
  license: GPL-2.0
  homepage: "https://www.xfce.org"
  tags:
    - desktop

platforms:
  - os: linux
    arch: x86_64
  - os: linux
    arch: aarch64

install:
  script:
    install: "install.sh"
    timeout: "600"

depends_on: []
