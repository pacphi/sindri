{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.sindri.dev/v4/policy.json",
  "title": "InstallPolicy",
  "description": "Sindri v4 install policy — placeholder",
  "type": "object",
  "properties": {
    "preset": { "type": "string", "enum": ["default", "strict", "offline"] }
  }
}
